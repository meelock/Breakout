package com.meelock.test;

public class Paddle {

    private static int paddleX = 0;

    public static void movePaddle() {

//        if (Gdx.input.getX() > 0 && Gdx.input.getX() < BrickBreaker.APPLICATION_WIDTH - PADDLE_WIDTH)
//            paddleX = Gdx.input.getX();
//        if (Gdx.input.getX() > BrickBreaker.APPLICATION_WIDTH - PADDLE_WIDTH)
//            paddleX = BrickBreaker.APPLICATION_WIDTH - PADDLE_WIDTH;
    }
}
